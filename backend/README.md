# TODOs:

[] Send whole new .ply and re-render
[] Actualizacion dinamica de la nube en el cliente (stremear updates a PLYs)
[] Return point cloud for image and join in front
[] Websocket to stream continuous results (but remember no fixed metric scale!!)
